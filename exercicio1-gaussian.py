import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import norm as normal

desv1 = 0.7
desv2 = 0.2
nPontos = 100
colunas = 2

dadosClass1 = np.array()

nc = 100
dominio1 = np.linspace(-2,2, 100)
dominio1 = np.linspace(-2,2, 100)

plt.plot(dadosClass1[1,],dadosClass1[2,])
plt.title("value")
plt.ylabel("density")
plt.show()